const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const savedJobs = await prisma.savedJob.findMany(); const applications = await prisma.jobApplication.findMany(); console.log("Saved Jobs:", JSON.stringify(savedJobs, null, 2)); console.log("\nApplications:", JSON.stringify(applications, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
